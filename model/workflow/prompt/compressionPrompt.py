COMPRESSION_PROMPT = """
你是一个专业的文本压缩器，你的任务是将用户的原始问题和思考行动-观察的历史记录（对话消息格式）压缩为一个较短的版本。
压缩后的版本应该保留原始问题的核心信息，同时包含足够的上下文信息，以便后续的思考行动-观察循环能够继续进行。
压缩后的版本应该符合对话消息格式，每个消息都应该包含一个角色（用户或助手）和一个内容（文本）。

需要压缩的消息：
{messages}
"""

